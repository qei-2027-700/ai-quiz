root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/api"
  bin = "tmp/server"
  include_ext = ["go"]
  exclude_dir = ["tmp", "gen", "internal/db/gen"]
  delay = 200

[log]
  time = true
