version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/db/queries"
    schema: "DDL"
    gen:
      go:
        package: "db"
        out: "internal/db/gen"
        emit_json_tags: true
        emit_interface: true
        emit_exact_table_names: false
